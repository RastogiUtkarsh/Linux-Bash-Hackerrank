#Given three integers (, , and ) representing the three sides of a triangle, identify whether the triangle is scalene, isosceles, or equilateral. If all three sides are equal, output EQUILATERAL. Otherwise, if any two sides are equal, output ISOSCELES. Otherwise, output SCALENE.
read a 
read b
read c
if [ $a -eq $b -a $b -eq -$c -a $a -eq $c ]
then 
    echo "EQUILATERAL"
elif [ $a -eq $b -o $b -eq $c -o $c -eq $a ]
then 
    echo "ISOSCELES"
else
    echo "SCALENE"
fi
